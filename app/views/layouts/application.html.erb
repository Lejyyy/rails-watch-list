<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rails Watch List" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Rails Watch List">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA manifest %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
  </head>

  <body class="modern-dark">
    <%# Animated background %>
    <div class="bg-animation"></div>
    
    <div class="content-wrapper">
      <%= yield %>
    </div>

    <style>
      /* Modern Dark Theme - 2026 Style */
      :root {
        --black: #0a0a0a;
        --charcoal: #1a1a1a;
        --neon-yellow: #E7F200;
        --yellow: #FFD700;
        --gold: #D4AF37;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body.modern-dark {
        background-color: var(--black);
        color: #f5f5f5;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        min-height: 100vh;
        position: relative;
        overflow-x: hidden;
      }

      /* Animated background */
      .bg-animation {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        background: 
          radial-gradient(ellipse at 20% 20%, rgba(231, 242, 0, 0.08) 0%, transparent 50%),
          radial-gradient(ellipse at 80% 80%, rgba(255, 215, 0, 0.05) 0%, transparent 50%),
          var(--black);
      }

      .content-wrapper {
        position: relative;
        z-index: 1;
        min-height: 100vh;
        padding: 2rem;
      }

      /* Typography */
      h1, h2, h3, h4, h5, h6 {
        font-weight: 700;
        letter-spacing: -0.02em;
      }

      .display-4 {
        font-size: 3rem;
        font-weight: 800;
        background: linear-gradient(135deg, #fff 0%, var(--neon-yellow) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Modern cards */
      .card {
        background: rgba(26, 26, 26, 0.8);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 215, 0, 0.1);
        border-radius: 1rem;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .card:hover {
        border-color: rgba(231, 242, 0, 0.3);
        transform: translateY(-4px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4), 0 0 30px rgba(231, 242, 0, 0.1);
      }

      /* Modern buttons */
      .btn-neon {
        background: linear-gradient(135deg, var(--neon-yellow) 0%, var(--yellow) 100%);
        color: var(--black);
        border: none;
        font-weight: 700;
        padding: 0.875rem 2rem;
        border-radius: 50px;
        transition: all 0.3s ease;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-size: 0.875rem;
      }

      .btn-neon:hover {
        background: linear-gradient(135deg, var(--yellow) 0%, var(--gold) 100%);
        transform: translateY(-2px);
        box-shadow: 0 10px 30px rgba(231, 242, 0, 0.3);
        color: var(--black);
      }

      .btn-outline-neon {
        background: transparent;
        color: var(--neon-yellow);
        border: 2px solid var(--neon-yellow);
        font-weight: 600;
        padding: 0.75rem 1.5rem;
        border-radius: 50px;
        transition: all 0.3s ease;
      }

      .btn-outline-neon:hover {
        background: var(--neon-yellow);
        color: var(--black);
        transform: translateY(-2px);
        box-shadow: 0 10px 30px rgba(231, 242, 0, 0.3);
      }

      /* Modern badges */
      .badge-neon {
        background: rgba(231, 242, 0, 0.15);
        color: var(--neon-yellow);
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-weight: 600;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        border: 1px solid rgba(231, 242, 0, 0.3);
      }

      /* Form inputs */
      .modern-input {
        background: rgba(26, 26, 26, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #fff;
        border-radius: 0.75rem;
        padding: 1rem;
        transition: all 0.3s ease;
      }

      .modern-input:focus {
        background: rgba(26, 26, 26, 1);
        border-color: var(--neon-yellow);
        box-shadow: 0 0 0 3px rgba(231, 242, 0, 0.15);
        color: #fff;
        outline: none;
      }

      .modern-input::placeholder {
        color: #666;
      }

      /* Card headers with gradient */
      .header-gradient {
        background: linear-gradient(135deg, var(--neon-yellow) 0%, var(--gold) 100%);
        padding: 1.5rem;
        border-radius: 1rem 1rem 0 0;
      }

      /* Empty states */
      .empty-state {
        text-align: center;
        padding: 4rem 2rem;
      }

      .empty-state i {
        font-size: 4rem;
        color: rgba(231, 242, 0, 0.3);
        margin-bottom: 1rem;
      }

      /* Text colors */
      .text-neon {
        color: var(--neon-yellow);
      }

      .text-muted {
        color: #888 !important;
      }

      /* Link overrides */
      a {
        color: var(--neon-yellow);
        text-decoration: none;
        transition: color 0.3s ease;
      }

      a:hover {
        color: var(--yellow);
      }

      /* Alert overrides */
      .alert-warning {
        background: rgba(231, 242, 0, 0.1);
        border: 1px solid rgba(231, 242, 0, 0.3);
        color: var(--neon-yellow);
        border-radius: 0.75rem;
      }

      /* Responsive */
      @media (max-width: 768px) {
        .content-wrapper {
          padding: 1rem;
        }
        
        .display-4 {
          font-size: 2rem;
        }
      }

      /* Animations */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .animate-in {
        animation: fadeInUp 0.6s ease forwards;
      }

      /* Staggered animations */
      .col-12:nth-child(1) { animation-delay: 0.1s; }
      .col-12:nth-child(2) { animation-delay: 0.2s; }
      .col-12:nth-child(3) { animation-delay: 0.3s; }
      .col-12:nth-child(4) { animation-delay: 0.4s; }
    </style>
  </body>
</html>

